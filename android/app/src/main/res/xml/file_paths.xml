<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Tus rutas originales (mantener compatibilidad) -->
    <external-path name="my_images" path="." />
    <cache-path name="my_cache_images" path="." />
    
    <!-- Rutas adicionales para mejor funcionamiento -->
    <!-- Directorio interno de archivos -->
    <files-path name="internal_files" path="." />
    
    <!-- Directorio externo de archivos de la app -->
    <external-files-path name="external_files" path="." />
    
    <!-- Cache externo -->
    <external-cache-path name="external_cache" path="." />
    
    <!-- Específico para archivos de audio -->
    <external-files-path name="audio_files" path="Audio/" />
    <files-path name="audio_internal" path="audio/" />
</paths>